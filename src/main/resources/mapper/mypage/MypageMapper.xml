<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.cavia.trader.module.member.mapper.MyPageMapper">
    <select id="findByMemberId" resultType="io.cavia.trader.module.member.dto.GameParticipationDto">
        select
        id,
        member_id as memberId,
        game_id as gameId,
        return_rate as returnRate,
        game_rank as gameRank,
        earned_score as earnedScore,
        post_score as postScore,
        earned_cash as earnedCash,
        post_cash as postCash,
        entered_at as enteredAt
        from game_participations
        where member_id = #{memberId}
        order by entered_at ASC
    </select>
</mapper>