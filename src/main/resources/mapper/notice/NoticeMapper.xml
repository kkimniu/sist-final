<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.cavia.trader.module.notice.mapper.NoticeMapper">
    <insert id="saveNotice" parameterType="io.cavia.trader.module.notice.dto.NoticeDto">
        insert into notices (
        title,
        content,
        created_at,
        updated_at,
        pinned
        )
        values(
        #{title},
        #{content},
        #{createdAt},
        #{updatedAt},
        #{pinned}
        )
    </insert>
    <select id="noticeListAll" resultType="io.cavia.trader.module.notice.dto.NoticeDto">
        select *
        from notices
    </select>
    <select id="existsById" parameterType="int" resultType="int">
        select count(*)
        from notices
        where id = #{id}
    </select>
    <delete id="deleteNotice" parameterType="io.cavia.trader.module.notice.dto.NoticeDto">
        delete
        from notices
        where id = #{id}
    </delete>
    <update id="updateNotice" parameterType="io.cavia.trader.module.notice.dto.NoticeDto">
        update
        <set>
            <if test="title != null">
                title=#{title},
            </if>
            <if test="content != null">
                content=#{content},
            </if>
            updated_at=${updatedAt},
            <if test="pinned != null">
                pinned=#{pinned},
            </if>
        </set>
        where id=#{id}
    </update>
</mapper>